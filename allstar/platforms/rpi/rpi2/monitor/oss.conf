; generated from ../channels/chan_oss.c

[general]

device = /dev/dsp	; This is the onboard sound for the RPi2
extension = 1999	; default extension to call Change to your node number
context = console	; default context for outgoing calls

